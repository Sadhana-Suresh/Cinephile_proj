<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cinephile Login</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #141E30, #243B55);
      font-family: Arial, sans-serif;
      color: #fff;
    }
    .login-box {
      background: rgba(0, 0, 0, 0.6);
      padding: 30px;
      border-radius: 12px;
      text-align: center;
      width: 300px;
    }
    .login-box h2 {
      margin-bottom: 20px;
    }
    .login-box input {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border-radius: 8px;
      border: none;
      outline: none;
    }
    .login-box button {
      background: #ff4b2b;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 8px;
      width: 100%;
      cursor: pointer;
      transition: 0.3s;
    }
    .login-box button:hover {
      background: #ff3b1f;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Login to Cinephile</h2>
    <input type="text" id="username" placeholder="Enter username" required>
    <input type="password" id="password" placeholder="Enter password" required>
    <button onclick="login()">Login</button>
  </div>

  <script>
    function login() {
      const user = document.getElementById('username').value.trim();
      const pass = document.getElementById('password').value.trim();

      if (user && pass) {
        // Save login session in localStorage
        localStorage.setItem('cinephileUser', user);

        // Redirect to main Cinephile site
        window.location.href = "cinephile.html";
      } else {
        alert("Please enter both username and password!");
      }
    }
  </script>
</body>
</html>
